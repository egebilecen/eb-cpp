#include <string>
#include <sstream>
#include <vector>
#include <utility>

namespace EB
{
    namespace String
    {
        std::vector<std::string> split(std::string const& s, char delim);
    }
}
